import {createTheme} from '@rneui/themed';

export const theme = createTheme({
  lightColors: {
    primary: '#800000',
    secondary: '#FFFFFF',
  },
  darkColors: {
    primary: '#800000',
    secondary: '#FFFFFF',
  },
  components: {
    Button: {
      raised: true,
    },
  },
});
